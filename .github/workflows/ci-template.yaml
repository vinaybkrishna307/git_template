name: CI on Develop

on:
  push:
    branches:
      - develop


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: echo "Installing..."

      - name: Run tests
        run: echo "Running tests..."

      - name: Build
        run: echo "Building..."